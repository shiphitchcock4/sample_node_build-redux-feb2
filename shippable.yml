# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  - 0.6

env:
  - M1=one
  - M2=two
  
matrix:
 allow_failure:
   - node_js: 0.6
    

build:
  
   ci:
    - ps -eax
    - ls -al
    - echo $M1 and $M2
    - npm install
    - npm test

  

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
      
