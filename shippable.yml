# Language setting
language: node_js

# Version number
node_js:
  - 0.12
  - 0.10

env:
  - M1=one
  - M2=two
  
matrix:
 allow_failure:
   - node_js: 0.12
    

build:
  
   ci:
    - ps -eax
    - ls -al
    - echo $M1 and $M2
    - if [ "$SHIPPABLE_NODE_VERSION" == "0.12" ]; then foobar; fi
    - npm install
    - npm test

  

integrations:
  hub:
    - integrationName: ship-docker
      type: docker
      
